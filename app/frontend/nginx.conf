server {
    listen 80;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    # Proxy API requests to the Python backend using a variable
    location /api/ {
        proxy_pass http://${BACKEND_HOST}:5000/api/;
    }
}